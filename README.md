# Demo code for TDD in Python

## Setup
- Create a venv with `python3 -m venv .venv` and activate with `source .venv/bin/activate`
- Install the dependencies `pip3 -r requirements.txt`
- Run the tests `pytest`